## Export const: Preamble

```jsx
export const labs =   [
    { title: 'Fill the PL initial form', link: '/labs/github-alu-form' },
    { title: 'Learn to use VsCode', link: '/labs/visual-studio-code' }
  ];

<div>
{JSON.stringify(labs)}
</div>
```

export const labs =   [
    { title: 'Fill the PL initial form', link: '/labs/github-alu-form' },
    { title: 'Learn to use VsCode', link: '/labs/visual-studio-code' }
  ];

<div>
{JSON.stringify(labs)}
</div>