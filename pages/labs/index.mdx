# Labs for PL

import Link from 'next/link'
import styles from '../index.module.css'

**Labs** section for PL subject.


1. [Fill the PL initial form](/labs/github-alu-form)
2. [Learn to use VSCode](/labs/visual-studio-code)
3. Learn $$\LaTeX{}$$: $$x^2$$

## Dynamic Table example
<table>
  <thead>
    <tr>
      <th>Country</th>
      <th>Flag</th>
    </tr>
  </thead>
  <tbody>
    {[
      { country: 'France', flag: '🇫🇷' },
      { country: 'Ukraine', flag: '🇺🇦' }
    ].map(item => (
      <tr key={item.country}>
        <td>{item.country}</td>
        <td>{item.flag}</td>
      </tr>
    ))}
  </tbody>
</table>
    

<ul>
{
  [
    { title: 'Fill the PL initial form', link: '/labs/github-alu-form' },
    { title: 'Learn to use VsCode', link: '/labs/visual-studio-code' }
  ].map((item, index) => (
    <li key={item.link}>* <Link href={item.link} className={styles.cta}>{item.title}</Link> [{item.title}]({item.link})</li>)
  )
}
</ul>
